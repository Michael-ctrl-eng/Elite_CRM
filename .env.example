# ═══════════════════════════════════════════════════
# Elite CRM - Vercel Environment Variables
# ═══════════════════════════════════════════════════
# Add ALL of these in Vercel → Project → Settings → Environment Variables

# ─── DATABASE (MySQL) ───
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# Password special chars must be URL-encoded (e.g. + becomes %2B)
DATABASE_URL=mysql://user:password@host:3306/database

# ─── NEXTAUTH ───
# Set to your production URL
NEXTAUTH_URL=https://crm.elitepartnersus.com
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=generate-a-secret-with-openssl-rand-base64-32

# ─── APP CONFIG ───
NEXT_PUBLIC_SIGNUP_ENABLED=false
NEXT_PUBLIC_APP_NAME=Elite CRM
NEXT_PUBLIC_DEMO_MODE=false

# ─── SMTP (Email) ───
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_SENDER=noreply@yourdomain.com
SMTP_USER=noreply@yourdomain.com
SMTP_PASS=your-smtp-password
